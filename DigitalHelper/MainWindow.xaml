<Window x:Class="DigitalHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalHelper"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Title="MainWindow" Height="600" Width="1000">
    <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="35">
        <Frame x:Name="MainFrame" Source="/Views/MainMenu.xaml"
               NavigationUIVisibility="Hidden">
        </Frame>
        <!--<Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Margin="10">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Vertical" >
                    <Image Height="200" Width="200" Stretch="Uniform">
                        <Image.Source>
                            <BitmapImage UriSource="/Resources/vault.png" DecodePixelWidth="200"></BitmapImage>
                        </Image.Source>
                    </Image>
                        <TextBlock HorizontalAlignment="Center" FontSize="30"
                               Text="Password Vault"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="1" Grid.Row="0" Margin="10">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Vertical" >
                    <Image Height="200" Width="200" Stretch="Uniform">
                        <Image.Source>
                            <BitmapImage UriSource="/Resources/chat.png" DecodePixelWidth="200"></BitmapImage>
                        </Image.Source>
                    </Image>
                    <TextBlock HorizontalAlignment="Center" FontSize="30"
                               Text="Custom Help"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="0" Grid.Row="1" Margin="10">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Vertical" >
                    <Image Height="200" Width="200" Stretch="Uniform">
                        <Image.Source>
                            <BitmapImage UriSource="/Resources/settings.png" DecodePixelWidth="200"></BitmapImage>
                        </Image.Source>
                    </Image>
                    <TextBlock HorizontalAlignment="Center" FontSize="30"
                               Text="Settings"/>
                </StackPanel>
            </Button>

            <Button Name="exitButton" Grid.Column="1" Grid.Row="1" Margin="10"
                Click="exitButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="25"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Vertical" >
                    <Image Height="200" Width="200" Stretch="Uniform">
                        <Image.Source>
                            <BitmapImage UriSource="/Resources/exit.png" DecodePixelWidth="200"></BitmapImage>
                        </Image.Source>
                    </Image>
                    <TextBlock HorizontalAlignment="Center" FontSize="30"
                    Text="Exit"/>
                </StackPanel>
            </Button>
        </Grid>-->
    </Border>
</Window>
